@{
    Enabled = @(
        'aesthetic'
        'keymap'
        'prompt'
        'fuzzy'
        'navigation'
        'projects'
        'git'
        'ai'
        'history'
        'completions'
        'tools'
        'aliases'
        'utilities'
        'doctor'
        'knowledge'
    )
    Disabled = @(
        # 'cursor'  # removed — use modules/ai providers
    )
}
