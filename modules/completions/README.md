# completions

Registra `Register-ArgumentCompleter` para temas, AI providers y tools.

## Par CLI ↔ IDE

| IDE | CLI |
|-----|-----|
| IntelliSense | Tab en `Set-DsTheme`, `Invoke-DsAi -Provider`, `Get-DsTool` |
